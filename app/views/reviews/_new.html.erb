<%= simple_form_for [work_session, review] do |f| %>
  <%= f.input :content, label: false, as: :text, class: 'review_form' %>
  <%= f.submit "submit", class: "btn btn-primary center-block"%>
<% end %>
