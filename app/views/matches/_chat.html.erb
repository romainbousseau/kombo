<%= simple_form_for [match, message], remote: true  do |f| %>
  <%= f.input :content, as: :text%>
  <%= f.submit "Send", class: "btn btn-primary", id: "message-send", controller: :messages, action: :create %>
<% end %>
